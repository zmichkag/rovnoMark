global:
  scrape_interval: 2s # Каждые 2 секунды забираем данные для высокой точности графиков скорости

scrape_configs:
  - job_name: 'rovno-gateway-test'
    static_configs:
      - targets: ['rovno-app-test:8080'] # Стучимся во внутренний порт тестового контейнера