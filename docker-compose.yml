services:
  rovnomark:
    build: .
    container_name: rovnomark-app
    # Теперь мы явно передаем флаги при запуске контейнера
    command: ["/bin/marking-service", "--port=8080", "--debug"]
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: always